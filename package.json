{
  "scripts": {
    "test": "nodemon --exec 'npm run build && npm run lint && node dist/index.js' --watch src --ext ts",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "lint": "eslint . --ext .ts",
    "tw": "npx tailwindcss -i style.tailwind.css -o ./public/css/style.min.css --watch --minify"
  },
  "dependencies": {
    "dotenv": "^16.4.4",
    "ejs": "^3.1.9",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.19",
    "@typescript-eslint/eslint-plugin": "^7.0.1",
    "@typescript-eslint/parser": "^7.0.1",
    "eslint": "^8.56.0",
    "nodemon": "^3.0.3"
  }
}
